import React from 'react'

type Props = {
    paramsHeading?: boolean,
    heading: string
}

const MobileWrapper = ({paramsHeading}: Props) => {
  return (
    <main>
        <header>
            {}
        </header>
        <footer></footer>
    </main>
  )
}

export default MobileWrapper